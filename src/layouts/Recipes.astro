---
import "../assets/styles/global.css";
import Navbar from "../components/layout/Navbar";
export interface Props {
  title: string;
}

const { frontmatter } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{frontmatter.title}</title>
  </head>
  <body>
    <Navbar />
    <main>
      <div id="content-container" class="flex justify-center items-center">
        <article id="recipe" class="p-4 w-[40rem] prose">
          <h1
            class="text-4xl font-bold font-merriweather capitalize text-center"
          >
            {frontmatter.title}
          </h1>
          {
            frontmatter.image ? (
              <img
                src={frontmatter.image}
                alt={frontmatter.title}
                title={frontmatter.title}
              />
            ) : null
          }
          <slot />
          <h2>Tags:</h2>
          <ul class="">
            {
              frontmatter.tags.map((tag: string) => (
                <li class="text-3xl capitalize">
                  <a href={"/tags/" + tag}>{tag}</a>
                </li>
              ))
            }
          </ul>
          <a href={frontmatter.link}>YouTube Link</a>
        </article>
      </div>
    </main>
  </body>
</html>
